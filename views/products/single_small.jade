if product
	.product.product--small.well.well-sm
		p.product-name
			a(href='/products/' + product.slug)
				= product.name
		p.product-image
			a(href='/products/' + product.slug)
				img.thumbnail.img-responsive(src='//' + product.image_small)
		p.product-price
			= product.prettyPrice()
